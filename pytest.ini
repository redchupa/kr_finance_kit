[pytest]
testpaths = tests
# pytest-homeassistant-custom-component's entrypoint imports `fcntl` (Unix only),
# so we disable auto-load on Windows. CI runs on Ubuntu and is unaffected
# (the `no:` prefix is a no-op when the plugin isn't installed).
addopts = -p no:homeassistant
asyncio_mode = auto
